rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /employees/{id} {
      allow read, write: if true;
    }
    match /payRunHistory/{id} {
      allow read, write: if true;
    }
  }
}
